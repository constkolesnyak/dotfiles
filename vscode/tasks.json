{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pymain",
            "command": "pymain",
            "type": "shell", "options": { "shell": { "args": ["-c", "-i"] } }
        },
        {
            "label": "pyts",
            "command": "pyts",
            "type": "shell", "options": { "shell": { "args": ["-c", "-i"] } }
        },
        {
            "label": "pycov",
            "command": "pycov",
            "type": "shell", "options": { "shell": { "args": ["-c", "-i"] } }
        }
    ]
}
